# --- Secrets — TUYỆT ĐỐI không commit ---
.env
*.pem
*.key

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# --- Airflow runtime (sinh ra khi docker compose up, không phải source code) ---
logs/
plugins/
config/airflow.cfg
config/*.cfg
airflow.db
standalone_admin_password.txt

# --- Spark ---
spark-warehouse/
derby.log
metastore_db/

# --- Data / local artifacts (không commit dữ liệu thật hoặc file tạm) ---
# *.parquet
*.csv
/tmp/
scripts/output/

# --- Metabase local metadata (nếu chạy H2 file-based) ---
metabase-data/
metabase.db*

# --- OS / Editor ---
.DS_Store
.idea/
.vscode/
*.swp
